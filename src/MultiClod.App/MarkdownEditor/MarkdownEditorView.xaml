<UserControl x:Class="MultiClod.App.MarkdownEditor.MarkdownEditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:MultiClod.App.MarkdownEditor"
             mc:Ignorable="d"
             PreviewKeyDown="OnPreviewKeyDown">
    <Grid>
        <local:MarkdownRenderEditControl x:Name="RenderEdit" DirtyChanged="OnRenderEditDirtyChanged" EditModeChanged="OnRenderEditEditModeChanged">
            <local:MarkdownRenderEditControl.LeadingToolbarContent>
                <!-- Not x:Name'd - a Button set via a sibling control's content property lands in
                     that control's own NameScope, not this file's, so MarkdownEditorView.xaml.cs
                     grabs it as saveButton via RenderEdit.LeadingToolbarContent instead. -->
                <Button Content="Save" Width="60" Margin="0,0,12,0"
                        Visibility="Collapsed" IsEnabled="False" Click="OnSaveClick" />
            </local:MarkdownRenderEditControl.LeadingToolbarContent>
        </local:MarkdownRenderEditControl>
    </Grid>
</UserControl>
