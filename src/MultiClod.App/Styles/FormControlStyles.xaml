<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <!--
        Dark-theme-safe TextBox/Button/ComboBox/RadioButton styles, shared by Settings\SettingsView
        and Skills\SkillEditor\SkillEditorView (extracted out of SettingsView.xaml, which was their
        original home). ComboBox/ComboBoxItem need a full retemplate, not just Background/Foreground
        setters - the default Aero2 chrome draws its closed-box text and item-hover highlight via
        internal SystemColors-bound brushes a plain Setter can't reach, which would otherwise leave
        this dark-on-dark and unreadable.
    -->
    <Style TargetType="TextBox">
        <Setter Property="Background" Value="{DynamicResource Theme.Brush.ControlBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.PrimaryForeground}" />
        <Setter Property="CaretBrush" Value="{DynamicResource Theme.Brush.PrimaryForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border}" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="VerticalContentAlignment" Value="Center" />
    </Style>

    <Style TargetType="Button">
        <Setter Property="Background" Value="{DynamicResource Theme.Brush.ControlBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.PrimaryForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border}" />
        <Setter Property="Padding" Value="10,6" />
    </Style>

    <Style TargetType="ComboBoxItem">
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBoxItem">
                    <Border x:Name="Bd" Background="{DynamicResource Theme.Brush.ControlBackground}" Padding="{TemplateBinding Padding}">
                        <ContentPresenter />
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsHighlighted" Value="True">
                            <Setter TargetName="Bd" Property="Background" Value="{DynamicResource Theme.Brush.Accent}" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="ComboBox">
        <Setter Property="Background" Value="{DynamicResource Theme.Brush.ControlBackground}" />
        <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.PrimaryForeground}" />
        <Setter Property="BorderBrush" Value="{DynamicResource Theme.Brush.Border}" />
        <Setter Property="Padding" Value="8,6" />
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="ComboBox">
                    <Grid>
                        <ToggleButton x:Name="ToggleButton" Focusable="False" ClickMode="Press"
                                      Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}"
                                      IsChecked="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}, Mode=TwoWay}">
                            <ToggleButton.Template>
                                <ControlTemplate TargetType="ToggleButton">
                                    <Border Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="1">
                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*" />
                                                <ColumnDefinition Width="24" />
                                            </Grid.ColumnDefinitions>
                                            <Path Grid.Column="1" Data="M0,0 L4,4 L8,0 Z" Fill="{DynamicResource Theme.Brush.PrimaryForeground}"
                                                  HorizontalAlignment="Center" VerticalAlignment="Center" />
                                        </Grid>
                                    </Border>
                                </ControlTemplate>
                            </ToggleButton.Template>
                        </ToggleButton>
                        <ContentPresenter x:Name="ContentSite" IsHitTestVisible="False"
                                          Content="{TemplateBinding SelectionBoxItem}"
                                          ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}"
                                          TextElement.Foreground="{TemplateBinding Foreground}"
                                          Margin="{TemplateBinding Padding}"
                                          HorizontalAlignment="Left" VerticalAlignment="Center" />
                        <!-- Required part name for IsEditable="True" (e.g. SkillEditorView's model
                             field) - ComboBox looks it up by this exact name to wire up live
                             typing. Without it, an editable ComboBox's internal state (including
                             the popup) behaves unpredictably rather than just failing to show
                             typed text. Hidden unless IsEditable, via the trigger below. -->
                        <TextBox x:Name="PART_EditableTextBox" Visibility="Hidden" Focusable="True"
                                 Background="Transparent" Foreground="{TemplateBinding Foreground}" BorderThickness="0"
                                 Margin="{TemplateBinding Padding}" HorizontalAlignment="Left" VerticalAlignment="Center" />
                        <Popup x:Name="Popup" Placement="Bottom" AllowsTransparency="True" Focusable="False"
                               PopupAnimation="Slide" IsOpen="{TemplateBinding IsDropDownOpen}">
                            <Border Background="{DynamicResource Theme.Brush.ControlBackground}" BorderBrush="{DynamicResource Theme.Brush.Border}" BorderThickness="1"
                                    MinWidth="{Binding ActualWidth, RelativeSource={RelativeSource TemplatedParent}}" MaxHeight="200">
                                <ScrollViewer>
                                    <ItemsPresenter />
                                </ScrollViewer>
                            </Border>
                        </Popup>
                    </Grid>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsEditable" Value="True">
                            <Setter TargetName="PART_EditableTextBox" Property="Visibility" Value="Visible" />
                            <Setter TargetName="ContentSite" Property="Visibility" Value="Hidden" />
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- No retemplate - the default chrome's small indicator circle reads fine against either theme's background. -->
    <Style TargetType="RadioButton">
        <Setter Property="Foreground" Value="{DynamicResource Theme.Brush.PrimaryForeground}" />
        <Setter Property="Margin" Value="0,0,0,6" />
    </Style>
</ResourceDictionary>
