<UserControl x:Class="MultiClod.App.Skills.SkillDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             PreviewKeyDown="OnPreviewKeyDown">
    <Grid Background="#1E1E1E">
        <!--
            View mode (default) and edit mode are mutually exclusive by Visibility, same rule as
            MainWindow's own canvas children - Document is only ever replaced, never the control
            itself, so nothing here needs add/remove semantics.
        -->
        <FlowDocumentScrollViewer x:Name="MarkdownViewer" Background="#1E1E1E" Padding="16" />

        <TextBox x:Name="RawEditor" Visibility="Collapsed" Background="#1E1E1E" Foreground="White"
                 CaretBrush="White" FontFamily="Consolas" FontSize="13"
                 AcceptsReturn="True" AcceptsTab="True" TextWrapping="NoWrap"
                 VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                 BorderThickness="0" Padding="16"
                 TextChanged="OnRawEditorTextChanged" />

        <StackPanel HorizontalAlignment="Right" VerticalAlignment="Top" Margin="8">
            <ToggleButton x:Name="EditToggle" Content="Edit" Width="60" Click="OnEditToggleClick" />
            <Button x:Name="SaveButton" Content="Save" Width="60" Margin="0,4,0,0"
                    Visibility="Collapsed" IsEnabled="False" Click="OnSaveClick" />
        </StackPanel>
    </Grid>
</UserControl>
